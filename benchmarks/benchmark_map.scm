(define (range n)
  (if (= n 0) '() (cons n (range (- n 1)))))
(define (map f lst)
  (if (null? lst)
      '()
      (cons (f (car lst)) (map f (cdr lst)))))
(define (square x) (* x x))
(define numbers (range 1000))
(display (car (map square numbers)))
